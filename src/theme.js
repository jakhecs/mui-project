import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#1760a5",
      light: "lightblue",
    },
    secondary: {
      main: "#15c630",
    },
    myTheme: {
      main: "red",
    },
  },
});
